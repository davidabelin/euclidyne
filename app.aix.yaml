runtime: python314
service: euclidyne
entrypoint: gunicorn --worker-tmp-dir /tmp -b :$PORT euclidyne:app

env_variables:
  APP_BASE_PATH: /euclidyne
  AIX_HUB_URL: https://aix-labs.uw.r.appspot.com/
  FLASK_SECRET_KEY: ""

service_account: aix-admin@aix-labs.iam.gserviceaccount.com
